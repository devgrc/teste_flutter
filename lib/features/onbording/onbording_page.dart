import 'package:flutter/material.dart';

class OnboardngPage extends StatelessWidget {
  const OnboardngPage({super.key, required AppBar appBar});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}